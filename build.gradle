description = 'Grails Augmented Resources Plugin'

version = '1.0.1-SNAPSHOT'

group = 'at.scheinecker'
ext.artifactId = 'augmented-resources-plugin'

ext.gradleVersion = '1.5'

buildscript {
    repositories {
        mavenCentral()
    }
    dependencies {
    }
}

apply plugin: 'java'
apply plugin: 'groovy'
apply plugin: 'idea'
